import {createContext} from "react";

export const FavoriteContexts = createContext(null);
export const RegisteredUsersContext = createContext(null);
export const LoggedInUserContext = createContext(false);
export const UserObjectContext = createContext(null);